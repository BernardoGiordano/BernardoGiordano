[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # starlette 1.6 asks for httpx2; the pinned httpx still drives TestClient, and
    # swapping that dependency is not this suite's business.
    ignore::starlette.exceptions.StarletteDeprecationWarning
